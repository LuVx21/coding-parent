package org.luvx.common.cursor;

import static org.junit.jupiter.api.Assertions.*;

import org.luvx.common.util.User;

class CursorIteratorTest {
    void m1() {
 //         * CursorIterator<Integer, User> users = CursorIterator.newGenericBuilder()
 //                *         .start(1)
 //                *         .bufferSize(100)
 //                *         .cursorExtractor(User::getId)
 //                *         .buildEx(UserRepository::findAllUsers);
 // * // 直接作为Stream访问
 // * users.stream().forEach(user -> {
 // *     // 访问单个用户
 // * });
 // * }
    }
}
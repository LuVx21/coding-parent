<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.luvx</groupId>
        <artifactId>coding-root</artifactId>
        <version>${revision}</version>
    </parent>

    <groupId>org.luvx.boot</groupId>
    <artifactId>coding-parent-boot-java</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <properties>
        <spring.boot.version>3.4.3</spring.boot.version>

        <redisson.boot>3.45.0</redisson.boot>
        <rocketmq.boot>2.3.0</rocketmq.boot>
    </properties>

    <modules>
        <module>coding-parent-boot-kotlin</module>
        <module>xxx-boot-starter</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.luvx.boot</groupId>
            <artifactId>logging-parent-spring</artifactId>
        </dependency>
        <dependency>
            <groupId>org.luvx.coding</groupId>
            <artifactId>coding-common</artifactId>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>coding-parent-boot-dependencies</artifactId>
                <version>${spring.boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>boot-common</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>boot-aop</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>boot-web</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>boot-nosql-redis</artifactId>
                <version>${revision}</version>
            </dependency>
            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>boot-nosql-mongo</artifactId>
                <version>${revision}</version>
            </dependency>

            <dependency>
                <groupId>org.luvx.boot</groupId>
                <artifactId>logging-parent-spring</artifactId>
                <version>${revision}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring.boot.version}</version>
                <configuration>
                    <!--fork:如果没有该项配置,整个devtools不会起作用-->
                    <!--<fork>true</fork>-->
                </configuration>
            </plugin>
        </plugins>
    </build>

</project>
